# secrets & env
.env
.env.*
!.env.example
*.pem
*.key

# node / next
node_modules/
.next/
out/
dist/
build/

# capacitor native projects (regenerated with `npx cap add`)
.expo/
android/
ios/

# logs / os / editor
*.log
.DS_Store
.idea/
.vscode/
coverage/

# build guide (contains internal strategy -- must not be tracked)
SolarShield-Build-Guide-for-Bob.md

# private hosting/deployment guide (owner-only -- cloners must not host this project)
DEPLOY-VERCEL.md

# TypeScript build cache
tsconfig.tsbuildinfo
